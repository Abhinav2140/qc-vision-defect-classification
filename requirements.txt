torch>=2.1.0
torchvision>=0.16.0
timm>=1.0.0             # provides GitHub-hosted (not download.pytorch.org) pretrained weights
opencv-python>=4.9.0
pandas>=2.0.0
Pillow>=10.0.0
numpy>=1.24.0
scikit-learn>=1.3.0     # metrics: accuracy, precision/recall/F1, confusion matrix
matplotlib>=3.7.0       # plots in the report notebook

# Needed only to rebuild/re-execute the report notebook:
# jupyter>=1.0.0
# nbformat>=5.9.0
# nbconvert>=7.0.0

# Optional, only needed if you wire up real hardware actuators:
# pymodbus>=3.6.0        # for ModbusActuator (PLC integration)
# RPi.GPIO>=0.7.1         # for GPIOActuator (Raspberry Pi relay control)
